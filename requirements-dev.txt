# Apex Quant — development dependencies

pytest>=8.0.0
pytest-cov>=4.1.0        # coverage plugin (drives --cov in pyproject addopts)
coverage>=7.4.0          # `coverage report -m` step in CI
ruff>=0.3.0
mypy>=1.9.0
pre-commit>=3.6.0        # local git hooks (see .pre-commit-config.yaml)
detect-secrets>=1.4.0    # secrets hook used by pre-commit
