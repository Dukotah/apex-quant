# secrets — NEVER COMMIT
.env
.env.local
*.key
*.pem

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
.venv/
venv/

# state is committed by the bot intentionally, but ignore local scratch
state/*.tmp
logs/*.tmp

# downloaded market data — regenerable via scripts/fetch_yahoo.py, don't commit
data/real/

# os
.DS_Store

# govcon research: regenerable fetched data
research/govcon/cache/
